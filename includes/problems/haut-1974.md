??? problem "展开：题意、思路、证明、复杂度与 C++17 代码"
    **题目**：这是一道签到题吗？

    [打开 HAUTOJ 原题 ↗](<https://acm.haut.edu.cn/problem.php?id=1974>){ .problem-source target="_blank" rel="noopener" }

    **题意**：根据给定输入，输出最终矩阵，具体形式参照输出样例。

    **核心思路**：这是一道签到题吗？ 用双层循环模拟即可，注意特殊输出的判断。

    **正确性**：构造只使用题目允许的元素与操作。；每一步都维持长度、取值、相邻关系或总和等约束。；结束时规模达到要求，因此输出是完整合法答案。

    **复杂度**：通常为 O(n)

    **易错点**：检查 0/1 起下标、n = 1、首尾元素和数组容量。；样例只是一个合法答案；本地检查约束，不要逐字比对。；按上界估算中间量，相关变量不要退回 int。

    - 核验状态：C++17 编译通过；1 个可机读公开样例/合法构造校验通过

    **C++17 实现**

    ```cpp linenums="1"
    #include <bits/stdc++.h>
    using namespace std;
    typedef long long ll;
    const int P = 1e9 + 7;
    const int N = 1e2 + 10;
    int a[N][N];
    int main() {
        int n, m;
        cin >> n >> m;
        int num = 0;
        for (int i = 1; i <= n; i++) {
            for (int j = 1; j <= m - 1; j++) {
                num++;
                cout << num << ' ';
            }
            num++;
            if (i % 2 == 1)
                cout << "HAUT" << endl;
            else
                cout << num << endl;
        }
        return 0;
    }
    ```
