??? problem "展开：题意、思路、证明、复杂度与 C++14 代码"
    **题目**：矩阵乘法

    [打开 HAUTOJ 原题 ↗](<https://acm.haut.edu.cn/problem.php?id=1230>){ .problem-source target="_blank" rel="noopener" }

    **题意**：给你两个n*n的矩阵，请计算并输出它们相乘的结果。

    **核心思路**：矩阵乘法中结果 C&#91;i&#93;&#91;j&#93; 是 A 第 i 行与 B 第 j 列的点积： `Σ A&#91;i&#93;&#91;k&#93;*B&#91;k&#93;&#91;j&#93;`。用三层循环枚举行、列和公共下标。每行输出时 仅在第 2 个及以后元素前加空格，避免多余分隔。累加使用 long long。

    **正确性**：扫描前保存的信息与已经处理的输入完全对应。；每读入一个元素，分支覆盖其全部情况并正确更新累计状态。；扫描结束时所有输入恰好处理一次，累计状态即为所求。

    **复杂度**：O(n³) 时间、O(n²) 空间

    **易错点**：按上界估算中间量，相关变量不要退回 int。

    - 核验状态：C++14 编译通过；1 个可机读公开样例/合法构造校验通过

    **C++14 实现**

    ```cpp linenums="1"
    #include <bits/stdc++.h>
    using namespace std;
    int main() {
        ios::sync_with_stdio(false);
        cin.tie(nullptr);
        int n;
        cin >> n;
        vector<vector<long long>> a(n, vector<long long>(n));
        vector<vector<long long>> b(n, vector<long long>(n));
        for (auto& row : a)
            for (long long& x : row)
                cin >> x;
        for (auto& row : b)
            for (long long& x : row)
                cin >> x;
        for (int i = 0; i < n; ++i) {
            for (int j = 0; j < n; ++j) {
                long long value = 0;
                for (int k = 0; k < n; ++k)
                    value += a[i][k] * b[k][j];
                if (j)
                    cout << ' ';
                cout << value;
            }
            cout << '\n';
        }
        return 0;
    }
    ```
