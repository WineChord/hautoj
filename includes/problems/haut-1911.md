??? problem "展开：题意、思路、证明、复杂度与 C++14 代码"
    **题目**：时间加速器

    [打开 HAUTOJ 原题 ↗](<https://acm.haut.edu.cn/problem.php?id=1911>){ .problem-source target="_blank" rel="noopener" }

    **题意**：要你求出完成所有任务花费的时间。

    **核心思路**：时间加速器使用总数是否小于等于mid即可。

    **正确性**：枚举范围包含每个可能答案，没有候选被遗漏。；判断条件与题目约束等价，所以只接受合法候选。；按题意计数、取最优或输出这些候选即可得到答案。

    **复杂度**：通常为 O(n)；若循环嵌套或迭代范围不同，应按代码重新核算

    **易错点**：按上界估算中间量，相关变量不要退回 int。

    - 核验状态：C++14 编译通过；1 个可机读公开样例/合法构造校验通过

    **C++14 实现**

    ```cpp linenums="1"
    #include <iostream>
    #define int long long
    using namespace std;
    int n, a, b, x[600006];
    bool check(int mid) // 判断函数
    {
        int tot = 0;
        for (int i = 1; i <= n; i++) {
            if (x[i] <= mid * a)
                continue;
            else {
                if ((x[i] - mid * a) % b == 0)
                    tot += (x[i] - mid * a) / b;
                else
                    tot += ((x[i] - mid * a) / b + 1);
            }
        }
        return tot <= mid;
    }
    signed main() {
        cin >> n >> a >> b;
        for (int i = 1; i <= n; i++)
            cin >> x[i];
        int l = 0, r = 1e9, mid;
        while (l <= r) // 二分搜索
        {
            mid = (l + r) / 2;
            if (check(mid))
                r = mid - 1;
            else
                l = mid + 1;
        }
        cout << l << endl;
        return 0;
    }
    ```
