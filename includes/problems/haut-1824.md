??? problem "展开：题意、思路、证明、复杂度与 C++14 代码"
    **题目**：jxh和lhy的决斗

    [打开 HAUTOJ 原题 ↗](<https://acm.haut.edu.cn/problem.php?id=1824>){ .problem-source target="_blank" rel="noopener" }

    **题意**：jxh和lhy两位聚聚想要来若干场游戏中的决斗来证明自己比对方强。假设攻击力高的一方先手（攻击力相等时jxh聚聚先手），给你每一场战斗时两位聚聚角色的属性和他们的操作，请你判断每一场胜负。

    **核心思路**：先按攻击力决定先后手（相等时 jxh 先手），每回合输入的两个操作依次 属于先手、后手。行动前令自己的护盾剩余回合减一，归零时清空护盾。 操作 1 普攻；2 回血但不超过上限；3 将护盾重置为 y、持续 3； 4 在 MP 足够时扣 z 并连续普攻三次。每次攻击先扣护盾再扣血，先手 行动造成死亡后该回合立即结束。模拟结束仍都存活即平局。

    **正确性**：扫描前保存的信息与已经处理的输入完全对应。；每读入一个元素，分支覆盖其全部情况并正确更新累计状态。；扫描结束时所有输入恰好处理一次，累计状态即为所求。

    **复杂度**：O(T·n) 时间、O(1) 额外空间

    **易错点**：按上界估算中间量，相关变量不要退回 int。

    - 核验状态：C++14 编译通过；1 个可机读公开样例/合法构造校验通过

    **C++14 实现**

    ```cpp linenums="1"
    #include <bits/stdc++.h>
    using namespace std;
    struct Player {
        long long hp, maxHp, mp, attack, heal, shieldValue, skillCost;
        long long shield = 0;
        int shieldTurns = 0;
        string name;
    };
    void beginTurn(Player& player) {
        if (player.shieldTurns > 0)
            --player.shieldTurns;
        if (player.shieldTurns == 0)
            player.shield = 0;
    }
    void hit(const Player& attacker, Player& defender) {
        long long damage = attacker.attack;
        long long absorbed = min(defender.shield, damage);
        defender.shield -= absorbed;
        defender.hp -= damage - absorbed;
    }
    void act(Player& actor, Player& opponent, int op) {
        if (op == 1) {
            hit(actor, opponent);
        } else if (op == 2) {
            actor.hp = min(actor.maxHp, actor.hp + actor.heal);
        } else if (op == 3) {
            actor.shield = actor.shieldValue;
            actor.shieldTurns = 3;
        } else if (op == 4 && actor.mp >= actor.skillCost) {
            actor.mp -= actor.skillCost;
            for (int repeat = 0; repeat < 3; ++repeat)
                hit(actor, opponent);
        }
    }
    int main() {
        ios::sync_with_stdio(false);
        cin.tie(nullptr);
        int T;
        cin >> T;
        while (T--) {
            Player jxh, lhy;
            cin >> jxh.hp >> jxh.mp >> jxh.attack >> jxh.heal >> jxh.shieldValue >>
                jxh.skillCost;
            cin >> lhy.hp >> lhy.mp >> lhy.attack >> lhy.heal >> lhy.shieldValue >>
                lhy.skillCost;
            jxh.maxHp = jxh.hp;
            lhy.maxHp = lhy.hp;
            jxh.name = "jxh";
            lhy.name = "lhy";
            Player* first = &jxh;
            Player* second = &lhy;
            if (jxh.attack < lhy.attack)
                swap(first, second);
            int rounds;
            cin >> rounds;
            string winner;
            for (int round = 0; round < rounds; ++round) {
                int op1, op2;
                cin >> op1 >> op2;
                if (!winner.empty())
                    continue;
                beginTurn(*first);
                act(*first, *second, op1);
                if (second->hp <= 0) {
                    winner = first->name;
                    continue;
                }
                beginTurn(*second);
                act(*second, *first, op2);
                if (first->hp <= 0)
                    winner = second->name;
            }
            if (winner.empty())
                cout << "-_-\n";
            else if (winner == "jxh")
                cout << "不愧是你啊，jxh！\n";
            else
                cout << "lhy！lhy！lhy！！！\n";
        }
        return 0;
    }
    ```
