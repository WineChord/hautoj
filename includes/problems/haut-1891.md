??? problem "展开：题意、思路、证明、复杂度与 C++17 代码"
    **题目**：简单的运算

    [打开 HAUTOJ 原题 ↗](<https://acm.haut.edu.cn/problem.php?id=1891>){ .problem-source target="_blank" rel="noopener" }

    **题意**：根据给定输入，输出仅包含一行 ， 第一行一个字符串是符合要求的符号式子 ， 符号之间不需要空格隔开

    **核心思路**：简单的运算 简单的字符串题目，输入后遍历字符串，找出不是数字的元素即可

    **正确性**：扫描前保存的信息与已经处理的输入完全对应。；每读入一个元素，分支覆盖其全部情况并正确更新累计状态。；扫描结束时所有输入恰好处理一次，累计状态即为所求。

    **复杂度**：通常为 O(n)

    **易错点**：避免下标越界；getline 前要处理上一次格式化读入留下的换行。；检查 0/1 起下标、n = 1、首尾元素和数组容量。

    - 核验状态：C++17 编译通过；1 个可机读公开样例/合法构造校验通过

    **C++17 实现**

    ```cpp linenums="1"
    #include <bits/stdc++.h>
    using namespace std;
    int n;
    string s;
    int main() {
        cin >> n;
        cin >> s;
        vector<char> c;
        for (int i = 0; i < s.size(); i++) {
            if (s[i] < '0' || s[i] > '9')
                c.push_back(s[i]);
        }
        for (int i = 0; i < c.size(); i++)
            cout << c[i];
        return 0;
    }
    ```
