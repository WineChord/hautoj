??? problem "展开：题意、思路、证明、复杂度与 C++14 代码"
    **题目**：想带Ah去浪漫的土耳其

    [打开 HAUTOJ 原题 ↗](<https://acm.haut.edu.cn/problem.php?id=1493>){ .problem-source target="_blank" rel="noopener" }

    **题意**：是想和Ah一起的。旅行社为了节约成本，最少要多少辆出租车。

    **核心思路**：分别统计 1、2、3、4 人组。4 人组独占一车；3 人组尽量各配一个 1 人组；2 人组两两配车，若剩一个 2 人组再带最多两个 1 人组； 最后剩余 1 人组每四组一车。这个贪心始终优先填满最难安排的大组。

    **正确性**：每一步选择不占用额外未来资源，并达到当前可证明的最优边界。；任意最优解若首个选择不同，都可交换为本算法的选择而不使答案变差。；反复交换即可得到与算法相同的最优解，所以算法全局最优。

    **复杂度**：O(n) 时间、O(1) 空间

    **易错点**：先尝试找反例；无法给出交换论证或下界时，不要把直觉当证明。

    - 核验状态：C++14 编译通过；1 个可机读公开样例/合法构造校验通过

    **C++14 实现**

    ```cpp linenums="1"
    #include <bits/stdc++.h>
    using namespace std;
    int main() {
        ios::sync_with_stdio(false);
        cin.tie(nullptr);
        int n;
        cin >> n;
        int count[5] = {};
        for (int i = 0, size; i < n; ++i) {
            cin >> size;
            ++count[size];
        }
        int ans = count[4];
        ans += count[3];
        count[1] = max(0, count[1] - count[3]);
        ans += count[2] / 2;
        if (count[2] % 2) {
            ++ans;
            count[1] = max(0, count[1] - 2);
        }
        ans += (count[1] + 3) / 4;
        cout << ans << '\n';
        return 0;
    }
    ```
