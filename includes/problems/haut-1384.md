??? problem "展开：题意、思路、证明、复杂度与 C++17 代码"
    **题目**：数学（二）

    [打开 HAUTOJ 原题 ↗](<https://acm.haut.edu.cn/problem.php?id=1384>){ .problem-source target="_blank" rel="noopener" }

    **题意**：根据给定输入，对每行输入，输出其对应那个的八进制数，单独占一行。

    **核心思路**：数学（二）思路：从M的个位开始，每次取三位，转化成8进制数。举例： M=1001010111第一步： 取出111， M=1001010， 结果为7第二步：取出010， M=1001， 结果为27第三步：取出001， M=1， 结果为127第四步：取出1， M=0（M=0则结束）， 结果为1127最终结果为：1127参考代码

    **正确性**：推导把原过程转换为等价的公式或不变量。；算法直接计算该等价量，并使用足够宽的数据类型处理边界。；因此计算结果就是题目定义的答案。

    **复杂度**：通常为 O(n)；若循环嵌套或迭代范围不同，应按代码重新核算

    **易错点**：检查 gcd/lcm 的 0 值、负数和乘法溢出。

    - 核验状态：C++17 编译通过；1 个可机读公开样例/合法构造校验通过

    **C++17 实现**

    ```cpp linenums="1"
    #include <stdio.h>
    int main() {
        int i, n, m;
        scanf("%d", &n);
        for (i = 1; i <= n; i++) {
            int reg = 0;
            int x = 1;
            ;
            scanf("%d", &m);
            if (m == 0) {
                printf("0\n");
            } else {
                while (m > 0) {
                    int t = m % 1000;
                    int a, b, c; // 个，十，百位。
                    a = t % 10;
                    b = (t / 10) % 10;
                    c = (t / 100) % 10;
                    reg = reg + (a + b * 2 + c * 4) * x;
                    x = x * 10;
                    m = m / 1000;
                }
                printf("%d\n", reg);
            }
        }
    }
    ```
