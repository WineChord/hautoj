??? problem "展开：题意、思路、证明、复杂度与 C++14 代码"
    **题目**：结束了(hard version)

    [打开 HAUTOJ 原题 ↗](<https://acm.haut.edu.cn/problem.php?id=1724>){ .problem-source target="_blank" rel="noopener" }

    **题意**：“草原上有一个兔子王国，里面有n只兔子公民，编号为1到n，公民在每年劳作之后要向国王缴纳税款，每只兔子缴纳的税款和自己的编号相同。但是国王的收税库被施了魔法，只能接受能把自己的容量整除的数量。现在国王想尽可能多的收税，但是不想提供过多的容量，请你给出一个最小的容量数吧\~”

    **核心思路**：容量必须同时被 1 到 n 的每个整数整除，所以最小容量正是 lcm(1,2,…,n)。对每个质数 p，只需乘入不超过 n 的最大质数幂p\^k；把这些质数幂相乘并随时对 $10^9$+7 取模即可。先用线性筛得到全部质数，并用除法判断下一次乘 p 是否仍不超过 n，避免溢出。

    **正确性**：推导把原过程转换为等价的公式或不变量。；算法直接计算该等价量，并使用足够宽的数据类型处理边界。；因此计算结果就是题目定义的答案。

    **复杂度**：O(n) 筛法时间、O(n) 空间

    **易错点**：检查 gcd/lcm 的 0 值、负数和乘法溢出。；减法结果可能为负，必要时先加模数再取模。；按上界估算中间量，相关变量不要退回 int。

    - 核验状态：C++14 编译通过；1 个可机读公开样例/合法构造校验通过

    **C++14 实现**

    ```cpp linenums="1"
    #include <bits/stdc++.h>
    using namespace std;
    #pragma GCC optimize("O2")
    #define ll long long
    #define endl "\n"
    const int max_n = 1e8 + 100;
    const int inf = 0x3f3f3f3f;
    const int mod = 1e9 + 7;
    int primes[6000000], id = 0;
    bool used[max_n];
    void init(int n) {
        for (int i = 2; i <= n; ++i) {
            if (!used[i])
                primes[id++] = i;
            for (int j = 0; j < id && i * primes[j] <= n; ++j) {
                used[i * primes[j]] = true;
                if (i % primes[j] == 0)
                    break;
            }
        }
    }
    int main() {
        ios::sync_with_stdio(false);
        cin.tie(0);
        cout.tie(0);
        int n;
        cin >> n;
        init(n);
        ll ans = 1;
        for (int i = 0; i < id; ++i) {
            if (primes[i] > n)
                break;
            ll t = 1;
            while (t <= n)
                t *= primes[i];
            t /= primes[i];
            ans = ans * t % mod;
        }
        cout << ans << endl;
        return 0;
    }
    ```
