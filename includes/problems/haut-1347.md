??? problem "展开：题意、思路、证明、复杂度与 C++14 代码"
    **题目**：Choice的加法

    [打开 HAUTOJ 原题 ↗](<https://acm.haut.edu.cn/problem.php?id=1347>){ .problem-source target="_blank" rel="noopener" }

    **题意**：根据给定输入，输出A+B的结果

    **核心思路**：从两个十进制字符串的末尾开始逐位相加，同时维护进位 carry。 某个字符串已经用完时把该位当 0；当前位写入 (x+y+carry)%10， 新进位为整除 10 的结果。最后若仍有进位再补一位，并将倒序收集的 数字翻转。这样可处理近 1000 位的非负整数。

    **正确性**：扫描前保存的信息与已经处理的输入完全对应。；每读入一个元素，分支覆盖其全部情况并正确更新累计状态。；扫描结束时所有输入恰好处理一次，累计状态即为所求。

    **复杂度**：每组 O(max(|A|,|B|)) 时间与空间

    **易错点**：避免下标越界；getline 前要处理上一次格式化读入留下的换行。

    - 核验状态：C++14 编译通过；1 个可机读公开样例/合法构造校验通过

    **C++14 实现**

    ```cpp linenums="1"
    #include <bits/stdc++.h>
    using namespace std;
    string addStrings(const string& a, const string& b) {
        int i = (int)a.size() - 1;
        int j = (int)b.size() - 1;
        int carry = 0;
        string reversed;
        while (i >= 0 || j >= 0 || carry) {
            int x = i >= 0 ? a[i--] - '0' : 0;
            int y = j >= 0 ? b[j--] - '0' : 0;
            int sum = x + y + carry;
            reversed.push_back(char('0' + sum % 10));
            carry = sum / 10;
        }
        reverse(reversed.begin(), reversed.end());
        return reversed;
    }
    int main() {
        ios::sync_with_stdio(false);
        cin.tie(nullptr);
        string A, B;
        while (cin >> A >> B)
            cout << addStrings(A, B) << '\n';
        return 0;
    }
    ```
