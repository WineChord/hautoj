??? problem "展开：题意、思路、证明、复杂度与 C++17 代码"
    **题目**：圣诞节前前前夕

    [打开 HAUTOJ 原题 ↗](<https://acm.haut.edu.cn/problem.php?id=1947>){ .problem-source target="_blank" rel="noopener" }

    **题意**：根据给定输入，无

    **核心思路**：圣诞节前前前夕 签到题，注意int范围不够，开long long 即可，两种解法。

    **正确性**：题目没有输入，所有合法运行面对同一个固定任务。；程序输出的常量与题目要求逐字一致。；因此程序在所有运行中都得到正确结果。

    **复杂度**：通常为 O(n)；若循环嵌套或迭代范围不同，应按代码重新核算

    **易错点**：核对中英文标点、尾随空格和末尾换行。；按上界估算中间量，相关变量不要退回 int。

    - 核验状态：C++17 编译通过；公开样例不可机读，未执行

    **C++17 实现**

    ```cpp linenums="1"
    #include <bits/stdc++.h>
    using namespace std;
    const int N = 2e5 + 10;
    typedef long long ll;
    int main() {
        ll ans = 0;
        for (ll i = 1; i <= 20251125; i++)
            ans += i;
        cout << ans;
        return 0;
    }
    ```
