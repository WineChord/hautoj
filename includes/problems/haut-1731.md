??? problem "展开：题意、思路、证明、复杂度与 C++14 代码"
    **题目**：年少有为

    [打开 HAUTOJ 原题 ↗](<https://acm.haut.edu.cn/problem.php?id=1731>){ .problem-source target="_blank" rel="noopener" }

    **题意**：设17673学长在1处，C+++创始人在n处，在1~n的路上共有1,2,3...,n个车站，从车站i到j所需的路费为r(i,j) (1&lt;=i&lt;j&lt;=n)。17673学长正在研究哈密顿路径，没有时间规划怎么换乘车辆，需要你来规划路费最少的路线，但是17673学长只喜欢最短路，所以要求你规划的路线路程最短。

    **核心思路**：年少有为动态规划，搜索，最短路 动态规划法从起点递推从起点到这个点的最短距离，推到终点后直接输出结果即可 C

    **正确性**：状态保存当前阶段所需的全部信息，初始状态与空前缀或空选择一致。；转移枚举所有合法来源，因此不漏解；取最优值时也不会保留劣解。；处理完全部输入后，目标状态正好对应题目要求。

    **复杂度**：按一次完整遍历估算，通常为 O(n+m)

    **易错点**：检查 0/1 起下标、n = 1、首尾元素和数组容量。；核对有向/无向、重边、自环、不可达点和点编号范围。；初值不能默认全部可达；0/1 背包必须倒序遍历容量。

    - 核验状态：C++14 编译通过；1 个可机读公开样例/合法构造校验通过

    **C++14 实现**

    ```cpp linenums="1"
    #include <stdio.h>
    #include <string.h>
    int min(int a, int b) {
        if (a > b)
            return b;
        else
            return a;
    }
    int main() {
        int n;
        int a[201][201], dp[201];
        scanf("%d", &n);
        memset(a, 127, sizeof(a));
        for (int i = 1; i < n; i++) {
            for (int j = i + 1; j <= n; j++) {
                scanf("%d", &a[i][j]);
            }
        }
        for (int i = 1; i <= n; i++) {
            dp[i] = a[1][i];
        }
        for (int i = 2; i <= n; i++) {
            for (int j = 1; j < i; j++) {
                dp[i] = min(dp[i], dp[j] + a[j][i]);
            }
        }
        printf("%d", dp[n]);
    }
    ```
