??? problem "展开：题意、思路、证明、复杂度与 C++17 代码"
    **题目**：错位时空

    [打开 HAUTOJ 原题 ↗](<https://acm.haut.edu.cn/problem.php?id=1730>){ .problem-source target="_blank" rel="noopener" }

    **题意**：在这个赛季的n场ycpc亚洲区域赛中，kws 要带领他的攻壳机动队拿得这个赛季的奖牌。如果打的第一场kws拿到了奖牌，他们就不会打下一场了(要放松一下)，如果打的第一场没拿到奖牌，他们也再不会打了(心态炸了)。lzh和zjy表示要打比赛非常开心和激动，但是也十分害怕打铁，于是他们找到了空间大师sjp，想要知道是否会打铁，但sjp是空间大师，不知道是否会打铁，……

    **核心思路**：错位时空概率期望 结果要进行四舍五入 计算时不能计算出最终拿奖率后用100减，在每一个给出拿奖率后要直接转换为打铁率后再进行计算

    **正确性**：推导把原过程转换为等价的公式或不变量。；算法直接计算该等价量，并使用足够宽的数据类型处理边界。；因此计算结果就是题目定义的答案。

    **复杂度**：通常为 O(n)；若循环嵌套或迭代范围不同，应按代码重新核算

    **易错点**：乘积使用足够宽的数据类型；除法前处理除数为 0 和整数截断。

    - 核验状态：C++17 编译通过；1 个可机读公开样例/合法构造校验通过

    **C++17 实现**

    ```cpp linenums="1"
    #include <stdio.h>
    int main() {
        int n;
        int temp;
        double sum;
        scanf("%d", &n);
        for (int i = 1; i <= n; i++) {
            scanf("%d", &temp);
            sum += 100 - temp;
        }
        printf("%.f", sum / n);
    }
    ```
