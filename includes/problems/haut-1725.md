??? problem "展开：题意、思路、证明、复杂度与 C++14 代码"
    **题目**：结束了(easy version)

    [打开 HAUTOJ 原题 ↗](<https://acm.haut.edu.cn/problem.php?id=1725>){ .problem-source target="_blank" rel="noopener" }

    **题意**：现在你的任务就变成了求能把从1到n所有整数整除的最小整数。

    **核心思路**：G.H.结束了 标签：数论 G-easy H-hard题目意思是寻找&#91;1,n&#93;的所有数的最小公倍数。 在这道题之前，先说一个数学定理：唯一分解定理又叫算术基本定理。 定理中提到：对于任何一个大于1的正整数，都存在唯一的分解式: ，其中 n = p k 1 ∗ p k 2 ∗ . . . ∗ p n k n n = p_1^{k_1}*p_2^{k_2}*...*p_n^{k_n} n=p1k1​​∗p2k2​​∗...∗pnkn​​都是素数。 p i p_i pi​知道了这个定理之后，再去求1到n的最小公倍数就变成了求出小于n的质数的最高幂次的乘积。 由于数据过大，所以需要使用线性筛法，为了照顾大家线性筛的写法都给大家了。 筛出所有的素数之后，进行暴力枚举最高次幂就行了。

    **正确性**：推导把原过程转换为等价的公式或不变量。；算法直接计算该等价量，并使用足够宽的数据类型处理边界。；因此计算结果就是题目定义的答案。

    **复杂度**：o(false)

    **易错点**：检查 gcd/lcm 的 0 值、负数和乘法溢出。；检查 0/1 起下标、n = 1、首尾元素和数组容量。；按上界估算中间量，相关变量不要退回 int。

    - 核验状态：C++14 编译通过；1 个可机读公开样例/合法构造校验通过

    **C++14 实现**

    ```cpp linenums="1"
    #include <bits/stdc++.h>
    using namespace std;
    #pragma GCC optimize("O2")
    #define ll long long
    #define endl "\n"
    const int max_n = 1e8 + 100;
    const int inf = 0x3f3f3f3f;
    const int mod = 1e9 + 7;
    int primes[6000000], id = 0;
    bool used[max_n];
    void init(int n) {
        for (int i = 2; i <= n; ++i) {
            if (!used[i])
                primes[id++] = i;
            for (int j = 0; j < id && i * primes[j] <= n; ++j) {
                used[i * primes[j]] = true;
                if (i % primes[j] == 0)
                    break;
            }
        }
    }
    int main() {
        ios::sync_with_stdio(false);
        cin.tie(0);
        cout.tie(0);
        int n;
        cin >> n;
        init(n);
        ll ans = 1;
        for (int i = 0; i < id; ++i) {
            if (primes[i] > n)
                break;
            ll t = 1;
            while (t <= n)
                t *= primes[i];
            t /= primes[i];
            ans = ans * t % mod;
        }
        cout << ans << endl;
        return 0;
    }
    ```
