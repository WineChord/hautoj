??? problem "展开：题意、思路、证明、复杂度与 C++14 代码"
    **题目**：子序列和

    [打开 HAUTOJ 原题 ↗](<https://acm.haut.edu.cn/problem.php?id=1707>){ .problem-source target="_blank" rel="noopener" }

    **题意**：由于结果可能过大，请输出结果对1e9+7取模后的值。

    **核心思路**：对任意一个位置，选择它以后，其余 n-1 个位置都可独立选择或不选，所以该元素恰好出现在 2\^(n-1) 个子序列中。所有子序列元素和的总和等于序列总和乘 2\^(n-1)，全程对 $10^9$+7 取模。

    **正确性**：扫描前保存的信息与已经处理的输入完全对应。；每读入一个元素，分支覆盖其全部情况并正确更新累计状态。；扫描结束时所有输入恰好处理一次，累计状态即为所求。

    **复杂度**：O(n+log n) 时间、O(1) 额外空间

    **易错点**：减法结果可能为负，必要时先加模数再取模。；按上界估算中间量，相关变量不要退回 int。

    - 核验状态：C++14 编译通过；1 个可机读公开样例/合法构造校验通过

    **C++14 实现**

    ```cpp linenums="1"
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <math.h>
    const int MOD = 1e9 + 7, maxn = 1e6 + 5;
    int n, a[1000005];
    long long BS[1000005];
    void init() {
        BS[0] = 1;
        for (int i = 1; i <= maxn - 1; i++) {
            BS[i] = (BS[i - 1] % MOD * 2) % MOD;
        }
    }
    int main() {
        init();
        scanf("%d", &n);
        long long res = 0;
        for (int i = 1; i <= n; i++) {
            scanf("%d", &a[i]);
            res = res % MOD + (a[i] % MOD * BS[n - 1] % MOD) % MOD;
        }
        printf("%lld\n", res);
        return 0;
    }
    ```
