---
title: "PID 1963 · 冥界的人员统计"
description: "新的逝者注册时，他将向系统发送一则内容为其名称的请求，如果该名称尚未存在于系统数据库内，则将该名称插入数据库，同时得到回应信息\"OK\"表示其已经成功登记。如果他请求的用户名已经存在于数据库内，那么系统将产生一个新的名称并将其加入数据库。新名称由逝者请求的名称与正整数i构成，i 为使 \"名称i\" 尚未存在于数据库内的最小的 i。"
tags: ["哈希与集合", "字符串", "模拟"]
---

# PID 1963 · 冥界的人员统计

[打开 HAUTOJ 原题 ↗](<https://acm.haut.edu.cn/problem.php?id=1963>){ .md-button .md-button--primary target="_blank" rel="noopener" }

!!! info "资料说明"
    本页是依据公开题面、公开解析与可复现代码核验整理的学习资料，不代表学校或校队的官方题解。

## 题目档案

| 项目 | 内容 |
|---|---|
| 训练定位 | 基础提高 |
| 主知识模块 | [数据结构](../topics/data-structures.md) |
| 知识标签 | 哈希与集合、字符串、模拟 |
| 时间 / 内存 | 1.000 S / 128 MB |
| 历史统计 | 49 次通过 / 113 次提交（43.4%） |
| 代码来源 | 依据公开题面与解析独立改写 |
| 核验状态 | C++17 编译通过；1 个可机读公开样例/合法构造校验通过 |

接受率只反映抓取时的历史提交快照，不等于官方难度或选拔分数线。

## 周赛出处

- [河南工大2025新生周赛（4）——命题人：张梦淇](../contests/2025/1105.md) · G 题 · 49/113

## 题意与输入输出

**题意摘要**：新的逝者注册时，他将向系统发送一则内容为其名称的请求，如果该名称尚未存在于系统数据库内，则将该名称插入数据库，同时得到回应信息"OK"表示其已经成功登记。如果他请求的用户名已经存在于数据库内，那么系统将产生一个新的名称并将其加入数据库。新名称由逝者请求的名称与正整数i构成，i 为使 "名称i" 尚未存在于数据库内的最小的 i。

**输入要点**：第一行一个整数 n(1≤n≤103)。接下来 n 行，每行表示逝者向系统发出的一则请求。每行内容均非空且均为由至多 32 个小写拉丁字母组成的字符串。

**输出要点**：共n 行，每行表示系统对一则请求做出的回应。如果该名称尚未存在于系统数据库内，则输出 OK 。如果用户请求的用户名已经被注册，则输出依照规则生成的新名。

## 零基础先修

- set 有序去重，unordered_set 平均 O(1) 查询；先判断是否需要顺序。
- string 可按下标访问字符；注意空串、大小写、标点和 getline 与 cin 的衔接。
- 列出状态变量与更新时间，严格按照题面规定的事件顺序更新。

## 思路推导

1. 按输入说明读取数据，并用最小样例手算一次，确认每个变量的含义。
2. 列出状态变量并按题面顺序更新；构造题同时维护每条输出约束。
3. 核心处理：哈希表 count&#91;name&#93; 既表示名字已注册，也保存下次应尝试的后缀。 首次出现输出 OK 并把计数设为 1；再次出现输出 name+count，再把计数 加一。按该题的标准数据，系统生成名不会与未来原始名产生冲突；若要 做通用版，可把生成名也写入表中。
4. 按题目要求输出，并检查最小值、最大值、重复值、单元素和格式边界。

## 正确性说明

- 初始变量与题面初始状态相同。
- 若某一步前状态正确，本步按相同顺序和规则更新后仍保持一致。
- 由归纳法，全部步骤结束时程序状态与答案都正确。

## 复杂度

期望 O(总字符数) 时间、O(n) 空间

## 易错点

- 避免下标越界；getline 前要处理上一次格式化读入留下的换行。
- 按规定顺序更新，避免本轮新状态在同一轮被重复使用。

## 题目摘要与 C++17 参考实现

--8<-- "includes/problems/haut-1963.md"

## 公开样例

### 样例 1

**输入**

```text
4
abacaba
acaba
abacaba
acab
```

**输出**

```text
OK
OK
abacaba1
OK
```


## 训练动作

- 遮住代码，用 3～5 句话复述状态、步骤和复杂度，再独立实现。
- 自行补三个边界：最小规模、极端或全部相等、恰好卡在条件等号处。
- 将本题归档到“数据结构”，一周后限时重做。

## 链接与来源

- [HAUTOJ 原题](<https://acm.haut.edu.cn/problem.php?id=1963>)
- [公开题解/参考来源 1](<https://www.cnblogs.com/hautacm/p/19239714>)

---

<div class="problem-nav" markdown="1">
[← PID 1961](1961.md)
[返回题目索引](index.md)
[PID 1964 →](1964.md)
</div>
